name: btl_nhom_15
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  # Database & Storage
  sqflite: ^2.3.0
  path: ^1.8.0
  shared_preferences: ^2.2.3

  # State management
  provider: ^6.1.2

  # Lịch & Âm dương
  table_calendar: ^3.1.2
  lunar: ^1.0.4

  # Biểu đồ thống kê
  fl_chart: ^0.69.0

  # Thông báo local
  flutter_local_notifications: ^17.2.4
  timezone: ^0.9.4

  # Mở bản đồ / link ngoài
  url_launcher: ^6.3.0

  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.3
  google_sign_in: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true